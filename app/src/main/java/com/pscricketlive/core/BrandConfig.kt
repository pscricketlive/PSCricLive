package com.pscricketlive.core

/**
 * Single source of truth for brand identity.
 * Change values here to rebrand the app without touching any screen code.
 */
object BrandConfig {
    const val APP_NAME = "PSCricketLive"
    const val TAGLINE = "Score & Broadcast"

    // Used on splash screen, about page, share-card watermark, etc.
    const val SHORT_NAME = "PS"
}
